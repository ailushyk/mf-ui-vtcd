import { Header } from './Header';

export default {
  component: Header,
  title: 'Components/Header',
};

const Template = () => {
  return <Header>Example Header</Header>;
};

export const Simple = Template.bind({});
